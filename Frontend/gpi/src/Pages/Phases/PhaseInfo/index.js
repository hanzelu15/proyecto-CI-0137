import React, { useState } from "react";

export const PhaseInfo = ({ phase }) => {
  const [isEditable, setIsEditable] = useState(false);
  const [loading, setLoading] = useState(true);
  return <div>PhaseInfo</div>;
};
